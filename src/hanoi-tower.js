module.exports = function calculateHanoi(disksNumber, turnsSpeed) {
    let A = Math.pow(2, diskNumber) - 1;
    let B = A / (trunSpeed / 3600);
    let result = {
      turns: A,
      seconds: B
    }
    return result;
}